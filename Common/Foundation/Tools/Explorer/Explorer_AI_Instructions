/*
 
> ai instructions
    > work only in the folder "ExplorerTests",
      under the namespace "Galleon.Checkout.ExplorerTests".
    > take a look at the file "AI_Instructions.txt" for explanation about project structure and entities.
    > take a look at the file "AI_code_style_instructions.txt" for examples of the code style.

> base classes
    > create a base class "ExplorerVisualEntity" inheriting VirtualEntity and Implementing "IEntity"
        > it has a reference to an ExplorerItem
    > Create a class "ExplorerItem" that inherits ExplorerVisualEntity.
    > Create a class "Title" that inherits ExplorerVisualEntity.
    > Create a class "Indicator" that inherits ExplorerVisualEntity.
    > Create a class "FoldoutArrow" that inherits ExplorerVisualEntity.
    > Create a class "Panel" that inherits ExplorerVisualEntity.
    > Create a class "Field" that inherits ExplorerVisualEntity.
    > ExplorerItem will hold a title, a list of indicators, a list of foldout arrows, a list of panels


> Target
    > ExplorerItem will hold member "Target" of type object.


> UI 
    > (right now, we will only use a few of the UI elements.) 
    > The explorer item will (visually) contain the folowing (only) :
        > Title
    > The explanation for each element is written here later on.

> UI policy 
    > There will be a method to build the UI (and update the members)
    > there built ui will only refresh its content and specific elements may be added/removed if needed.
    > this is insted of destroying everything and building from scratch everytime we need to refresh the display.
    
> Refresh
    > each element has a reference to the explorer item, so when refreshing, tehre is no need to pass the explorer item around.

> UI Elements
    > Title
        > Will display a text - the ToString() of the target.
        > If the target is IEntity - will display the DisplayName of the entity. 


> Create a class TestExplorerWindow, that is an editor window.
    > it will be openeed from a menu item "Tools/Galleon/TestExplorer"
    > it will contain (and display) an explorer item who's Target is initialized to "Root.Instance"

*/
